#! /bin/sh

# helm.bin is the original helm binary
case $1 in
  show*)
    set -x
    pwd
    exec /usr/local/bin/helm.bin $@
    set +x
    ;;
  *)
    set -x
    pwd
    exec /usr/local/bin/helm.bin secrets $@
    set +x
    ;;
esac
